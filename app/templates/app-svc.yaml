apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Values.appName }}
  name: {{ .Values.appName }}-svc
  namespace: {{ .Values.namespace }}
spec:
  ports:
    - port: 3001
      targetPort: 3001
  selector:
    app: {{ .Values.appName }}
